## Copy this file to .env and fill values before starting the server
# Your Gmail address (sender)
GMAIL_USER=

# Your Gmail App Password (create via Google Account -> Security -> App passwords) or OAuth token
GMAIL_PASS=your_app_password

# Recipient email (where you want to receive contact messages). Defaults to GMAIL_USER if unset.
RECIPIENT_EMAIL=abhinav.mahapatra18@gmail.com

# Optional: server port
PORT=3000
